server:
  port: 8090

spring:
  datasource:
    url: jdbc:mysql://192.168.1.5:3306/todolist?useUnicode=true&characterEncoding=utf8
    username: root
    password: 123456
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
  flyway:
    baseline-on-migrate: true
springdoc:
  swagger-ui:
    path: /swagger-ui.html
